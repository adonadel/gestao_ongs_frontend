export type Paginate = {
    current_page: number,
    first_page_url: string,
    from: number,
    to: number,
    total: number,
    last_page: number,
    last_page_url: string,
    links: [],
    next_page_url: string,
  };